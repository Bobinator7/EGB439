#include <iostream>

using namespace std;

int main(int argc, char **argv) {
  
  //get first sample line
  
  //get color type (skip after BGR squared bracket)
  //get color data (add to list of pairs of tuples)
  
  //process color information 
  //e.g.: normalized colorspace, boundaries, histogram equalization
  //autogenerated color boundaries?

  return 0;
}
